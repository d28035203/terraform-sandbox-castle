# Terraform Sandbox Castle

Safe **Terraform/OpenTofu** playground: local state, random_pet, null_resource.

```bash
terraform init && terraform plan && terraform apply -auto-approve
```

No cloud bill. Learn plan/apply without fear.

MIT · 2025 · IaC
